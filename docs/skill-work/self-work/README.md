# self-work — territory (WORK coordination)

**Purpose:** One cross-lane steering surface per instance (`users/<id>/self-work.md`) for objectives, active threads, sequencing, and strategy.

This is WORK coordination, not identity truth.

---

## Clarity

- `docs/skill-work/` holds lane docs (`work-*` style territories in downstream instances).
- `self-work.md` is one cross-lane steering surface per instance.
- `self.md` is Record identity and uses gate flow for durable updates.

---

## Optional template module

Use [sync-pack/README.md](sync-pack/README.md) for a reusable manual sync pattern across instances.

